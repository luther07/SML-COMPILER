(* Interview question, write a function of type:
   string list -> string list list, which receives a list of strings and
   returns these strings grouped into separate lists of those strings which
   are anagrams/permutations of each other. *)
fun anagram input : string list =
   let samesize x:
   let sortstr x: string =
   let comparestr x: string, y: string =


